pages:
  image: node:latest
  stage: deploy
  script:
    - cd client
    - npm ci
    - npm run build
    - mv dist public
    - find public -type f -regex '.*\.\(htm\|html\|txt\|text\|js\|css\)$' -exec gzip -f -k {} \;
  artifacts:
    paths:
      - public
  only:
    - gitlab-pages
